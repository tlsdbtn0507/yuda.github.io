import css from '../../../css/write.module.css'

const WriteSelect = () => {

  return (
    <div className={css.selectWrapper}>
      <h2 className={css.sectionMent}>{ '오늘 하루는 어떠셨나요?' }</h2>
    </div>
  )
};

export default WriteSelect